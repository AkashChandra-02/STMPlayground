/*
 * lock.c
 *
 *  Created on: Dec 9, 2025
 *      Author: akash
 */



	// LDREX/STREX
	// __LDREXB(addr);
	// __STREXB((val), (addr)))
	// __CLREX();

int init_semaphore(semaphore* s){
	s->value = NULL;
	s->status = freed;
}

int lock(semaphore* s, uint32_t value){
	s->value = NULL;
	s->status = freed;
}

int unlock(){

}

int free(){

}
